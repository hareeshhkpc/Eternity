/**
 * 
 */
package com.hin.hl7messaging.svgexport;


/**
 * @author Administrator
 *
 */
public class SVGExportTest {

	/*@Test
	public void testSvgExport(){
		Transcoder transcoder = new PDFTranscoder();
		//Transcoder transcoder = new org.apache.fop.render.ps.PSTranscoder();

		//Setup input
		InputStream in=null;
		try{
		in = new java.io.FileInputStream(new File("src/test/resources/sample.svg"));
		TranscoderInput input = new TranscoderInput(in);

		//Setup output
		OutputStream out = new java.io.FileOutputStream(new
		File("src/test/resources/embed.pdf"));
		out = new java.io.BufferedOutputStream(out);
		TranscoderOutput output = new TranscoderOutput(out);

		//Do the transformation
		transcoder.transcode(input, output);

		out.close();


		in.close();
		}
		catch(Exception e)
		{
		e.printStackTrace();
		}
		finally
		{
		System.exit(0);
		}
	}*/
	
}
