#Cassandra database connection details. To be understood by the CassandraClient class
#Mon Apr 30 12:43:46 GMT+05:30 2012

# Port number where Cassandra DB Server is listening (default, no need to change)
cassandra.DB_PORT=6190

# Whether to use Thrift connection (should be true)
cassandra.DB_THRIFT_CONNECTION=true

# Name the Cassndra cluster
cassandra.DB_CLUSTER_NAME=HIN Cluster

# Database (keyspace) name. No need to change
cassandra.DB_KEYSPACE_NAME=HIN_ETERNITY

# IP address of the Cassandra DB Server (should be changed)
cassandra.DB_HOST=172.25.250.165

# Path where the message configuration files are located (always change upto "webapps" in the given path)
messageConfig.dirPath=E:\\icthealth-projects\\deployments\\testing\\apache-tomcat-7.0.11/webapps/hin-web/message-configuration

# Path where the Lucene index directory is located. (should be changed)
indexing.LUCENE_INDEX_DIR_PATH=E:\\icthealth-projects\\deployments\\testing\\data\\lucene\\LuceneDirectory

# Workflow deployment files directory (No need to change)
workflow.WORKFLOW_DEPLOYMENT_PATH=WEB-INF/workflow

# Absolute path to the hin-web application (must change)
tomcat.path=E:\\icthealth-projects\\deployments\\testing\\apache-tomcat-7.0.11/webapps/hin-web

# Directory where the attachment files of welcome message (email) are kept. (no need to change)
attachment.file=/resources/welcomeMessage

# Internal. No need to change
messageConfig.idXPath=//id[root='HIN_MSG_ID']/extension

# Internal. No need to change
messageConfig.docIdXPath=//id[root='HIN_DOC_ID']/extension

# Internal. No need to change
messageConfig.roleNameXPath=//id[root='ROLE_NAME']/extension

# Internal. No need to change
messageConfig.subscriberIdXPath=/*/*/id[root='SUBSCRIBER_ID']/extension

# Internal. No need to change
messageConfig.typeXPath=/message/@config

# Internal. No need to change
messageConfig.userNameIdXpath=//id[root='USERNAME']/extension

# Internal. No need to change
messageConfig.passwordXpath=//id[root='PASSWORD']/extension

# Internal. No need to change
messageConfig.participantXpath=//id[root='SUBSCRIBER_ID']/extension

# Internal. No need to change
messageConfig.taskIdXPath=//id[root='TASK_ID']/extension

# Internal. No need to change
messageConfig.taskOutComeXPath=//id[root='TASK_OUTCOME']/extension

# Internal. No need to change
messageConfig.taskAsigneeXPath=//id[root='TASK_ASIGNEE']/extension

# Internal. No need to change
messageConfig.registartionType=PRPA_MT201000HT03

# ProxyPass URL of Couch DB given in Apache HTTP server. Must be the IP and port (if any) of the Apache HTTP Server
couch.DB_PATH=http://172.25.250.165/couchdb/

# Whether the workflow need to be deployed ( Change only if the workflow definitions are to be deployed/re-deployed)
DEPLOY_WORKFLOW=false

FileAttachment.ATTACHMENT_DIR=E:\\icthealth-projects\\deployments\\testing\\apache-tomcat-7.0.11\\webapps\\hin-web\\file-attachments
FileAttachment.READ_BUFFER_SIZE=2048
